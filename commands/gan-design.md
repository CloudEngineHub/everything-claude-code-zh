---
description: 运行生成器/评估器设计循环，用于前端或视觉工作，具有有界迭代和评分机制。
---

从 $ARGUMENTS 中解析以下内容：
1. `brief` — 用户对要创建的设计的描述
2. `--max-iterations N` — （可选，默认 10）最大设计-评估循环次数
3. `--pass-threshold N` — （可选，默认 7.5）通过的加权分数（设计模式默认更高）

## GAN 风格设计 Harness

一个双智能体循环（生成器 + 评估器），专注于前端设计质量。没有规划器——brief 本身就是规范。

这与 Anthropic 在前端设计实验中使用的模式相同，他们在此看到了创意突破，比如带有 CSS 透视和门廊导航的 3D 荷兰艺术博物馆。

### 设置
1. 创建 `gan-harness/` 目录
2. 将 brief 直接写入 `gan-harness/spec.md`
3. 编写聚焦设计的 `gan-harness/eval-rubric.md`，对设计质量和原创性赋予更高权重

### 设计特定的评估标准
```markdown
### 设计质量 (权重: 0.35)
### 原创性 (权重: 0.30)
### 工艺 (权重: 0.25)
### 功能性 (权重: 0.10)
```

注意：原创性权重更高（0.30 vs 0.20）以推动创意突破。功能性权重较低，因为设计模式聚焦于视觉质量。

### 循环
与 `/project:gan-build` 阶段 2 相同，但：
- 跳过规划器
- 使用聚焦设计的评分标准
- 生成器提示强调视觉质量而非功能完整性
- 评估器提示强调"这能赢得设计奖吗？"而非"所有功能都工作吗？"

### 与 gan-build 的关键区别
生成器被告知："你的首要目标是视觉卓越。一个令人惊艳的半成品应用胜过一个功能齐全的丑陋应用。追求创意飞跃——不同寻常的布局、自定义动画、独特的色彩运用。"
